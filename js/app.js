const l=document.querySelector('.loader');window.addEventListener('load',()=>setTimeout(()=>l?.classList.add('done'),450));const t=document.querySelector('.nav-toggle'),n=document.querySelector('nav');t?.addEventListener('click',()=>n?.classList.toggle('open'));const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show');else if(e.boundingClientRect.top>0)e.target.classList.remove('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(e=>o.observe(e));document.querySelectorAll('.lane,.cta').forEach(e=>e.addEventListener('mousemove',x=>{if(innerWidth>800){const r=e.getBoundingClientRect();e.style.transform=`translate3d(${(x.clientX-r.left-r.width/2)*.015}px,${(x.clientY-r.top-r.height/2)*.015}px,0)`} }));